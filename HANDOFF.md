> **기준일:** 2026-08-12
> **상태:** 진행 중 — 멀티모니터/버튼 버그 **2차 수정**(원인 재진단) 완료·재빌드됨, 사용자 실테스트 대기

# 닦아(ddak-a) — 진행 상황 인수인계

## 뭘 만드는 앱인가
맥 키보드를 닦을 때 실수로 키 입력이 컴퓨터에 전달되지 않게, 앱 실행 중엔 키보드를 완전히 차단하는
macOS 유틸리티. 마우스/트랙패드는 그대로 살아있음.

## 확정된 스펙
- **이름**: 닦아 (ddak-a)
- **플랫폼**: macOS 12(Monterey)+ , **유니버설 바이너리**(Intel x86_64 + Apple Silicon arm64)
- **실행 흐름**: 앱 실행 → "닦아 실행할까요?" 확인창(예/아니오) → 손쉬운 사용(Accessibility) 권한 체크
  → 하늘색 전체화면(연결된 **모든 모니터** 커버) + "키보드 청소중..." 애니메이션 텍스트 + 카운트다운
  → 5분 뒤 자동 종료 **또는** "다 닦았어요" 버튼 클릭 시 즉시 종료
- **키보드**: 내장 + 블루투스 외장 키보드까지 전부 차단 (CGEventTap으로 keyDown/keyUp/flagsChanged/미디어키 삼킴)
- **마우스·트랙패드**: 차단 안 함 (이벤트마스크에서 애초에 제외했으므로 자동으로 통과됨)
- **화면잠금(비밀번호)**: 안 씀
- **배포**: 앱스토어 안 씀. 깃허브 / 검로드에서 다운로드 배포 예정 (아직 개발자 서명 인증서 없음 — 필요하면 논의)
- **테스트 기간 한정 안전장치** (2026-08-12 결정, 반영 완료):
  1. 5분 카운트다운과는 별도로 독립된 **1분(60초) 하드 세이프티 타이머** — 다른 로직이 다
     실패해도 1분 지나면 무조건 키보드 잠금 해제 + 앱 종료
  2. **Cmd+Option+Esc(강제 종료)만 예외로 통과** — 이 조합만 이벤트탭에서 막지 않고 시스템에 그대로 전달

## 파일 구조
- `Package.swift` — SPM 실행파일 타겟, macOS 12+
- `Sources/ddaka/main.swift` — 앱 전체 로직 (단일 파일, 267줄 → 안전장치+버그수정 반영 후 현재 약 280줄대)
- `Info.plist`, `build.sh` (유니버설 빌드 + `.app` 번들 패키징 + ad-hoc 코드사이닝)
- ⚠️ 예전에 있던 `Sources/ddaka2/`(임시 우회 파일)는 이번 세션에서 완전히 정리·삭제됨 — 더 이상 없음

## 지금까지 한 일 (2026-08-12, 최근 세션 기준)

1. **파일 잠금 문제(구 블로커 1) 해결됨** — 예전 세션에서 `Package.swift`, `main.swift`가
   `Operation not permitted`로 막혔던 이슈가 새 세션에서는 재현 안 됨. 읽기/쓰기/이름변경 다 정상.
2. 안전장치 2개(1분 하드 타이머, Cmd+Opt+Esc 예외) 반영된 완성 버전(`ddaka2/main.swift`)을
   원본 `Sources/ddaka/main.swift`에 병합 완료. `ddaka2` 폴더는 정리 후 삭제함.
3. `build.sh`에 코드사이닝 스텝 추가 완료:
   ```
   codesign --force --deep --sign - "$APP_BUNDLE"
   ```
4. `tccutil reset Accessibility com.vismotive.ddaka` 실행 — 권한 상태 초기화.
5. **1차 재빌드 + 사용자 테스트** → 사용자가 시스템 설정 > 손쉬운 사용에서 `ddak-a` 토글 확인,
   앱 실행 성공. 하지만 **버그 2개 발견됨**:
   - "다 닦았어요" 버튼이 안 보임
   - 모니터 2개 중 **한쪽만** 하늘색으로 덮이고, 나머지 한쪽은 평소 화면 그대로 보임
   - (마우스는 정상 작동 확인됨)
6. **원인 진단**: `NSScreen.main`을 "메인(메뉴바 있는) 모니터"로 착각해서 사용한 게 버그 원인으로
   추정됨. `NSScreen.main`은 실제로는 "그 순간 키보드 포커스를 가진 창이 속한 화면"이라, 확인창을
   클릭하는 시점의 포커스 위치에 따라 콘텐츠(글자+버튼)가 엉뚱한 화면 판정 로직에 걸렸을 가능성이 큼.
7. **수정 완료 (재빌드까지 됨, 사용자 재검증 아직 안 됨)**:
   - `Sources/ddaka/main.swift`의 `createOverlayWindows()`에서 메인 화면 판정을
     `NSScreen.main` → `NSScreen.screens.first`(문서상 "메뉴바 있는 실제 메인 모니터"로 보장됨)로 변경
   - 창 생성 시 `screen:` 파라미터를 안 넘기고, 생성 직후 `window.setFrame(screen.frame, display: true)`로
     좌표를 한 번 더 명시적으로 고정 (멀티모니터 좌표계 이슈 방어)
   - 메인 화면 창은 `makeKeyAndOrderFront`, 나머지 화면 창은 `orderFrontRegardless()`로 분리
   - "다 닦았어요" 버튼을 흰 배경(`layer.backgroundColor`) + 진한 남색 텍스트(`attributedTitle`)로
     명시적 스타일링 — 기본 버튼이 하늘색 배경 위에서 옅게 렌더링돼 안 보이던 문제 방어
8. 수정 반영 후 **재빌드 성공** (`bash build.sh` 정상 완료, 유니버설 바이너리 + ad-hoc 서명 확인됨:
   `codesign -dv` 결과 `Identifier=com.vismotive.ddaka`, `Signature=adhoc`, 해시는 재빌드로 갱신됨)
9. 재빌드된 `ddak-a.app`을 `open`으로 실행까지는 시켜놨으나, **사용자의 실제 테스트 결과(버튼이
   이제 보이는지, 두 모니터 다 덮이는지)를 확인받기 전에 세션이 끊김** — 이게 다음 세션 최우선 확인사항.

10. **🚨 2026-08-12 (새 세션) — 코드 정독 후 원인 재진단 + 2차 수정.** 위 7번의 수정은 원인을
    못 짚었을 가능성이 크다고 판단(아래가 더 유력한 원인). 6곳 수정 후 재빌드 성공.
    - **(a) 버튼·글자 실종의 진짜 원인 후보**: `NSScreen.screens`를 두 번 호출해서
      `screen == NSScreen.screens.first`로 비교했음. `NSScreen.screens`는 호출할 때마다 다른
      인스턴스를 돌려줄 수 있어 이 비교가 **항상 false**가 될 수 있고, 그러면 콘텐츠가
      **어느 화면에도 안 그려진다**. → 배열을 한 번만 받아 `index == 0`으로 판정하도록 변경.
    - **(b) 한쪽 모니터만 덮이는 문제**: `NSWindow.constrainFrameRect()`가 창을 화면 안쪽으로
      끌어당겨 보조 모니터 창이 메인 쪽으로 딸려왔을 가능성. → `OverlayWindow: NSWindow`
      서브클래스를 만들어 `constrainFrameRect`를 그대로 반환하도록 오버라이드,
      `canBecomeKey`/`canBecomeMain`도 true로(borderless 창은 기본 false).
    - **(c) 안전성 강화 — 콘텐츠를 모든 모니터에 표시**로 변경. 마우스가 어느 화면에 있든
      탈출 버튼을 누를 수 있어야 하므로. (덤으로 (a)의 "메인 화면 판정" 자체가 불필요해짐)
    - **(d) 1분 하드 세이프티가 사실은 하드하지 않았음**: `Timer`는 메인 런루프에 얹혀 있어
      앱이 멈추면 같이 멈춘다. → 타이머를 `.common` 런루프 모드로 등록 + **런루프와 무관한
      백그라운드 큐에서 65초(60+5) 뒤 `exit(0)`** 하는 최후의 보루 추가. 프로세스가 죽으면
      이벤트탭도 사라져 키보드는 무조건 복구됨.
    - **(e) Cmd+Opt+Esc 탈출구가 실제로는 안 먹혔을 가능성**: `flagsChanged`를 전부 삼켜서
      시스템이 "Cmd+Opt이 눌린 상태"를 모를 수 있었음. → Command/Option 키의 `flagsChanged`
      (keycode 54·55·58·61)만 통과시키도록 변경. Shift·Control·CapsLock은 계속 차단.
    - **(f) 잡버그**: 이벤트 통과 시 `passRetained` → `passUnretained`(누수), `finishCleaning()`
      중복 호출 방지 플래그, `applicationWillTerminate`에서 이벤트탭 해제,
      청소 중 모니터 연결/해제 시 오버레이 재구성(`didChangeScreenParametersNotification`).

## ⚠️ 코드사이닝 관련 참고
ad-hoc 서명(`codesign --sign -`)은 인증서가 없어서, **재빌드할 때마다 서명 해시가 바뀜**. macOS TCC가
이걸 "다른 앱"으로 인식해서 Accessibility 권한이 풀릴 수 있음 — 재빌드 후 권한 요청이 다시 뜨면
그냥 다시 승인하면 됨(정상적인 동작, 버그 아님). 이 문제 자체를 근본적으로 없애려면 나중에 유료
애플 개발자 계정으로 정식 서명·공증이 필요함(아래 "나중에 할 일" 참고).

11. **🚨 2026-08-12 (같은 세션) — 최적화 패스.** 먼저 측정: 바이너리 **약 300KB**, 컴파일 **경고 0개**,
    deprecated API 없음 → 덩치·빌드 쪽은 손댈 게 없고 **실행 중 동작**만 손봤다. 오너가 A+B+9번 선택.
    - **(A-1) 이벤트탭 콜백에서 매 이벤트마다 `Set<Int64>`를 새로 만들던 것 제거** → 파일 상단
      전역 상수 + `switch` 비교로 변경. 이 콜백은 키 입력마다 실행되고 **느리면 macOS가 탭을 강제로
      끊어버려 키보드가 순간 뚫린다** — 성능이자 안정성 문제라 우선순위 최상.
    - **(A-2) 불필요한 필드 조회 제거**: keycode를 타입 분기 안에서만 읽고, `event.flags`는 Esc일
      때만 읽는다(대부분의 키는 그 전에 걸러짐).
    - **(A-3) 타이머 `tolerance` 부여**: dot 0.15초 / countdown 0.1초. macOS가 깨우기를 묶어 처리해
      전력 절약. **세이프티 타이머엔 일부러 안 줌**(안전장치라 정확해야 하고, 한 번만 돌아 무의미).
    - **(B-4) 점 애니메이션 문자열 4종을 전역 `cleaningTitles`로 미리 생성** (0.6초마다 문자열 조립 X)
    - **(B-5) 카운트다운은 표시값이 바뀔 때만 갱신** (`lastCountdownText` 비교). 창 재생성 시 리셋함.
    - **(B-6) `setFrame(display: false)`** — 창을 띄우기 전 한 번, 띄우며 또 한 번 그리던 중복 제거
    - **(B-7) 배경색 이중 지정 정리** — `contentView.layer.backgroundColor` 제거, 창 배경색만 사용
      (`wantsLayer`는 버튼 둥근모서리 때문에 유지). 전체화면 크기를 한 번 더 채우던 작업이 사라짐.
    - **(9) NX_SYSDEFINED를 통째로 삼키던 것 → subtype 8(미디어 키)만 차단.** 이 통로엔 밝기·볼륨
      말고 다른 시스템 신호도 실려 와서, 예전 코드는 엉뚱한 신호까지 막을 수 있었다.
      ⚠️ **차단 동작이 바뀌었으므로 밝기·볼륨 키 차단을 반드시 재테스트할 것.**
    - 재빌드 성공(경고 0), 크기 307,872 bytes, ad-hoc 서명 확인. **눈에 보이는 UI 변화는 없어야 정상.**
    - ❌ 안 한 것: `strip`으로 심볼 제거 — 300KB에서 수십 KB 줄자고 크래시 원인 추적을 포기할 이유가 없음.

## 🧯 테스트 전 필독 — 갇혔을 때 빠져나오는 방법 (2026-08-12)
1. 마우스로 **"다 닦았어요"** 클릭 (이제 모든 모니터에 떠 있음)
2. **Cmd+Option+Esc** → 강제 종료 창에서 닦아 종료
3. 아무것도 안 해도 **60초 뒤 자동 해제**, 그래도 안 풀리면 **65초에 프로세스 강제 종료**
4. 그래도 안 되면 전원 버튼 길게 눌러 강제 재시동 (최후)

## 다음 세션 체크리스트 (우선순위 순)
- [ ] **최우선**: 2차 수정(위 10번)으로 멀티모니터/버튼 버그가 실제로 고쳐졌는지 재검증
  - 앱 실행 → 확인창 "예" → (권한 재요청 뜨면 승인 후 재실행) → 두 모니터 **다** 하늘색으로 덮이는지,
    **두 모니터 다** 흰 배경 "다 닦았어요" 버튼 + "키보드 청소중..." 글자가 보이는지 확인
  - 안 고쳐졌으면 다음 순서로 의심: ① 시스템 설정 > 데스크탑과 Dock > "디스플레이마다 개별 공간"
    설정 상태 ② 한쪽 모니터에 전체화면 앱이 떠 있었는지 ③ `NSScreen.screens`가 실제로 몇 개를
    돌려주는지 로그로 확인 (지금은 로그가 없으니 필요하면 `NSLog` 추가해서 Console.app으로 확인)
- [ ] 키보드 완전 차단 테스트 (아직 한 번도 실제로 테스트 안 됨 — 사용자가 "이거 고치면 볼게"라고
      미뤄둔 상태)
- [ ] **밝기·볼륨(미디어) 키 차단 재테스트** — 위 11번 (9)에서 차단 방식이 바뀌었음. F1/F2(밝기),
      F10~F12(볼륨), 재생/음소거 키를 눌러 **아무 반응 없어야** 정상
- [ ] "다 닦았어요" 버튼 클릭 시 즉시 종료되는지
- [ ] 1분 하드 세이프티 타이머 실제로 작동하는지 (아무 것도 안 하고 60초 기다려서 확인)
- [ ] Cmd+Option+Esc 강제종료 예외가 실제로 통과되는지
- [ ] 5분 카운트다운 텍스트/자동종료 정상 작동
- [ ] (선택) 앱 아이콘 디자인
- [ ] (나중) 배포용 정식 서명·공증 — 애플 개발자 계정 필요 여부 사용자와 상의 (위 코드사이닝 참고
      항목과 연결됨 — 유료 계정 있으면 재빌드마다 권한 풀리는 문제도 같이 해결됨)
